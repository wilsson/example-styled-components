import styled, { css } from 'styled-components';

export const Title = styled.section`
    color: black;
    font-weight: bold;
    font-size: 30px;
    margin-bottom: 10px;
    font-family: arial;
`;